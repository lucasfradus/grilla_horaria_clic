{
  "name": "horarios-hot-clic-api",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "dev:win": "py -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000"
  }
}
